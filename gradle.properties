# Gradle 运行参数
org.gradle.jvmargs=-Xmx4G -Xms512m
org.gradle.daemon=true
org.gradle.parallel=true

# 模组基本信息（通常由 build.gradle 直接读取，此处可省略）
mod_version=1.0.0
mod_group=com.yourmod
mod_id=bipedraidermod
minecraft_version=1.21
forge_version=51.0.33